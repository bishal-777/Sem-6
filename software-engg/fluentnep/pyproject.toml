[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fluentnep"
version = "0.1.0"
description = "Nepali code-mixed speech disfluency detector and real-time fluency coach"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
